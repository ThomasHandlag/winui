import 'package:flutter/material.dart';

class GitHubStatus extends StatelessWidget {
  const GitHubStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
